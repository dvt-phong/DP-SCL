# Documentation

- `technical_dp_scl_architecture_vi.md`: Vietnamese technical explanation of the DP-SCL architecture.
- `technical_loss_dp_scl_vi.md`: Vietnamese technical explanation of BCE, SupCon, and combined loss ablations.

The code is the source of truth for runnable arguments and defaults. Use `python train.py -h` and `python train_experiment.py -h` for the current CLI.
